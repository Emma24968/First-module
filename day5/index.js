const schedule = ['Sweep', 'Bath in the Morning', 'Eat', 'Go to school'];

schedule.push('Pay attention');

// const bath = schedule.indexOf('Bath in the Morning');
// console.log(bath);

const bay = schedule[2];
console.log(`I like to ${bay} like a baby`)
